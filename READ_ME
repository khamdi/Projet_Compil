Auteurs: HAMDI Karim, LUONG Guy.


Pour compiler et obtenir l'executable tpc, se placer dans le repertoire /src et faire make

Pour analyser un fichier de type .tpc -> ./tpc nomdufichier.tpc
	Les instructions en machine virtuelle sortent alors sur la sortie standard.

Pour obtenir un fichier_cible contenant les instructions en machine virtuelle il faut faire une redirection:
	./tpc nomdufichier.tpc > fichier_cible

Pour faire executer les instructions par la machine virtuelle il faut utiliser un pipe.
	./tpc nomdufichier.tpc | ./vmAMD64

tpc.lex est l'analyseur syntaxique écrit en flex

tpc.y est l'analyseur syntaxique écrit en bison

tpc.output contient les informations relatives au bison, c'est le résultat du bison en mode verbeux.

Il manque la gestion des tableaux.

Ce projet a été réalisé en étroite collaboration avec le binôme ANTEUR-FERNANDES
